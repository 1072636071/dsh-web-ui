packages:
  - .

# Peer APIs (cordis, react, @deepseek-ai/dsh-*) resolve at runtime from the
# dsh profile tree (module table / profile node_modules), never from this
# repo's own install — matching the turtle-ui convention.
autoInstallPeers: false

allowBuilds:
  esbuild: true
