/**
 * Code-K-line UI plugin, node half: provides no host-side behavior — the
 * plugin's substance is the browser registration in `./client`. The
 * invariant companion lives in `./invariant`.
 * @module @deepseek-ai/dsh-client-ui-code-kline
 */

/** Provides no host-side behavior. */
export function apply(): void {}
