# 设置卡导入引导 + welcome 触发

**Status:** resolved

**Blocked by:** 05

**构建内容：** 皮肤设置卡检测素材是否已导入（探测 /pet/jiangxiao/ 可用性）：未导入时显示 dsh-pet 导入引导（引导用户去 pet 设置卡导入 zip）；已导入时显示就绪状态。皮肤启用且素材就绪时本地触发一次 welcome 态（3s 驻留后回 idle）。设置卡同时承载五效 FX 开关 UI。用户视角：一眼看清素材状态、知道去哪导入；装好后首次启用有一次欢迎仪式。

**验收标准：**

- [x] 设置卡检测素材就绪状态（替代/修正现有 /api/pet/pets 检测逻辑）
- [x] 未导入：显示导入引导文案与入口（指向 dsh-pet 导入）；已导入：显示就绪
- [x] 检测失败/网络异常时保守视为未导入，不报错崩卡
- [x] 皮肤启用 + 素材就绪时触发一次 welcome，3s 驻留后回 idle；每次启用至多一次
- [x] 设置卡样式消费语义别名（FX/令牌体系内），无颜色字面量
- [x] 中英双语文案走包内 locales

## 评论

- 2026-08-19：已随首轮实现落地（commit aba4b8f）。导入引导、welcome 触发、FX 开关 UI 与设计基准无耦合；开关文案若因 FX 视觉返工（工单 03）需微调，由工单 03 一并处理。
